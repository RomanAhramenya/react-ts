import React from 'react'
import { useParams } from 'react-router-dom'
const Posts = () => {
    const { id } = useParams()
    return (
        <div>
            1
        </div>
    )
}

export default Posts
