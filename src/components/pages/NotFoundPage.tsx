interface INotFoundPageProps {

}

const NotFoundPage = (props: INotFoundPageProps) => {
    const { } = props

    return (
        <div>
            Not Found
        </div>
    )
}

export default NotFoundPage