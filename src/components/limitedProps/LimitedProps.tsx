import Button from "./Button"

interface ILimitedProps {

}

const LimitedProps: React.FC<ILimitedProps> = (props) => {
    const { } = props

    return (
        <>
            <Button string={'ddsd'} />
        </>
    )
}

export default LimitedProps
