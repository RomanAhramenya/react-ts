import { ElementType } from 'react'
interface IButtonPolimorfProps<E extends ElementType = ElementType> {
    as?: E
}

const ButtonPolimorf = (props: IButtonPolimorfProps) => {
    const { } = props

    return (
        <button>

        </button>
    )
}

export default ButtonPolimorf